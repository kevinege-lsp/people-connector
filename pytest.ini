[pytest]
testpaths = tests
# The package is imported from the repo root rather than an installed
# distribution, so put the root on sys.path explicitly. Without this, `pytest`
# (the console script) fails where `python -m pytest` happens to work.
pythonpath = .
